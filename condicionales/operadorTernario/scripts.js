/* 
Se utiliza cuando una condición será true o false, similar al if 
Su ejecución puede tener una o varias sentencias, en este caso iran separadas por comas y entre paréntesis.
*/

let num = 7;

(num % 2 == 0) ? console.log(`${num} es un número par`): 
                 console.log(`${num} es un número impar`);